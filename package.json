{
  "name": "@a11yfred/monorepo",
  "version": "2.2.0",
  "private": true,
  "packageManager": "npm@11.15.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "lint": "turbo run lint",
    "dev": "turbo run dev",
    "test": "turbo run test"
  },
  "devDependencies": {
    "turbo": "^2.9.14"
  },
  "engines": {
    "node": ">=18"
  }
}
